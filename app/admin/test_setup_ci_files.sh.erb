echo "====================================================================="
echo "This would clone the repo git@github.com:miatrinity/<%= app_name %>_ci_setup.git
echo "And copy all it's files to the new app in /tmp/<%= app_name %>/"
echo "====================================================================="
